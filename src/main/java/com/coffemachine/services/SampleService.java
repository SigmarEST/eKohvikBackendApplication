package com.coffemachine.services;

import com.coffemachine.model.ItemFeedback;
import javassist.NotFoundException;

public interface SampleService {

    void addSample(ItemFeedback itemFeedback);
}
